language: python
python:
    - 2.7
install:
    - pip install -r requirements-dev.txt --use-mirrors
    - pip install -e .
script:
    - py.test
after_script:
    - pip install --quiet --use-mirrors coveralls
    - coveralls
